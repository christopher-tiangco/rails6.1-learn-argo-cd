rails-master-key:
  sealed_secret_manifest_filename: rails-master-key-sealed-secret.yaml
  data:
  - gh_secret_name: RAILS_MASTER_KEY
    target_placeholder: ~ENCRYPTED_RAILS_MASTER_KEY~

postgres-production-uri:
  sealed_secret_manifest_filename: postgres-production-uri-sealed-secret.yaml
  data:
  - gh_secret_name: POSTGRES_PRODUCTION_URI
    target_placeholder: ~ENCRYPTED_URI~

redis-production-uri:
  sealed_secret_manifest_filename: redis-production-uri-sealed-secret.yaml
  data:
  - gh_secret_name: REDIS_PRODUCTION_URI
    target_placeholder: ~ENCRYPTED_URI~

test-cred:
  sealed_secret_manifest_filename: test-cred-sealed-secret.yaml
  data:
  - gh_secret_name: TEST_USERNAME
    target_placeholder: ~ENCRYPTED_USERNAME~
  - gh_secret_name: TEST_PASSWORD
    target_placeholder: ~ENCRYPTED_PASSWORD~

ghcr-secret:
  sealed_secret_manifest_filename: ghcr-secret-sealed-secret.yaml
  data:
  - gh_secret_name: GHCR_DOCKERCONFIGJSON
    target_placeholder: ~ENCRYPTED_DOCKERCONFIGJSON~